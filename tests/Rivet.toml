[cdn]
    site_id = "XXXX"

[matchmaker]
    [matchmaker.captcha]

[[matchmaker.lobby_groups]]
    name_id = "test"
    regions = ["abc123"]
    max_players_normal = 5
    max_players_direct = 5
    max_players_party = 5

        [[matchmaker.lobby_groups.runtime.docker]]
            build_id = ""
            args = ["a", "b", "c"]

                [[matchmaker.lobby_groups.runtime.docker.ports]]
                    label = "abc"
                    target_port = 80
                    proxy_protocol = "https"

