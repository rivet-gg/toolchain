cdn:
  build_command: echo 'Hello, world!' > dist/index.html
  build_output: dist

matchmaker:
  max_players: 16
  tier: basic-1d1

  docker:
    dockerfile: Dockerfile.test

  regions:
    lnd-atl: {}
    lnd-sfo: {}

  game_modes:
    default: {}

kv: {}

